import { LocalDataPipePipe } from './local-data-pipe.pipe';

describe('LocalDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
